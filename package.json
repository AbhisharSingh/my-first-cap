{
  "name": "my-first-cap",
  "version": "1.0.0",
  "description": "My First CAP Application",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": ">=8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": ">=1"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db.sqlite"
        }
      }
    }
  }
}